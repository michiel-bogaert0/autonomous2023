slam_controller:
  node_management:
    exploration:
      # SLAM
      - graphslam

    racing:
      # SLAM
      - slam_mcl

      # Control
      # - minimum_curvature

    always:
      # SLAM
      - lap_counter

      # Control
      - front_wheel_feedback_control
      - pathplanning
      - path_smoother

slam_mcl:
  path_to_map: "/ugr/car/map/slam/global"
lap_counter:
  finishpoint: [0, 0, 0]
speed:
  target: 2.0
  target_racing: 10.0
pure_pursuit:
  change_index: True
  minimal_distance: 1.2
  world_frame: "ugr/map"
mpc:
  change_index: True
  minimal_distance: 1.2
  world_frame: "ugr/map"
